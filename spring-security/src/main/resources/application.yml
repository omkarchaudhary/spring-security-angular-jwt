server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb?useSSL=false
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
user.app.jwtSecret: springbootsecretkey
user.app.jwtExpirationMs: 86400000